import React from 'react';
import './Background.css';
//Note that for other components to show up, they will need a higher z score than the background

const Background = () => {
  return (
    <div className="background">
      {/* other elements */}
    </div>
  );
}

export default Background;
